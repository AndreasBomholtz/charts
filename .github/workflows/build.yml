name: Build and push Docker image to dockerhub

on:
  push:
    branches:
      - 'main'

env:
  IMAGE_NAME: andreasbomholtz/development

jobs:

  build:
    runs-on: ubuntu-latest

    steps:
    - name: Check out the repo
      uses: actions/checkout@v4

    - name: Login to dockerhub
      uses: docker/login-action@v3
      with:
       username: ${{ secrets.DOCKERHUB_USERNAME }}
       password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Get next version
      uses: reecetech/version-increment@2024.4.3
      id: version
      with:
        scheme: semver
        increment: patch

    - name: Build and push to dockerhub
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: "${{ env.IMAGE_NAME }}:${{ steps.version.outputs.version }}"

    - name: Update Chart.yaml with new version
      uses: mikefarah/yq@master
      with:
        cmd: yq -i '.version = "${{ steps.version.outputs.version }}" | .appVersion = "${{ steps.version.outputs.version }}"' ./charts/12factor/Chart.yaml

    - name: Commit the changes to Chart.yaml
      uses: devops-infra/action-commit-push@master
      with:
        github_token: "${{ secrets.GITHUB_TOKEN }}"
        amend: true